{
  "variables": {
    "proxmox_url": "{{ env `PROXMOX_URL` }}",
    "proxmox_node": "{{ env `PROXMOX_NODE` }}",
    "proxmox_username": "{{ env `PROXMOX_USERNAME` }}",
    "proxmox_token": "{{ env `PROXMOX_TOKEN` }}",
    "template_name": "ubuntu-22.04-enterprise-app",
    "template_description": "Ubuntu 22.04 LXC template for Enterprise App deployment"
  },
  "builders": [
    {
      "type": "proxmox-clone",
      "proxmox_url": "{{ user `proxmox_url` }}",
      "node": "{{ user `proxmox_node` }}",
      "username": "{{ user `proxmox_username` }}",
      "token": "{{ user `proxmox_token` }}",
      "insecure_skip_tls_verify": false,
      
      "clone_type": "lxc",
      "template_id": 9000,
      "vm_name": "{{ user `template_name` }}",
      "vm_id": 9001,
      "description": "{{ user `template_description` }}",
      
      "memory": 2048,
      "cores": 2,
      "disk_size": "20G",
      
      "network_adapters": [
        {
          "bridge": "vmbr0",
          "model": "virtio"
        }
      ],
      
      "ssh_username": "root",
      "ssh_timeout": "15m"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "apt-get update",
        "apt-get install -y python3 python3-pip python3-venv git curl wget",
        "apt-get install -y postgresql-client redis-tools",
        "apt-get install -y build-essential libssl-dev libffi-dev python3-dev",
        "apt-get clean",
        "rm -rf /var/lib/apt/lists/*"
      ]
    },
    {
      "type": "shell",
      "inline": [
        "useradd -m -s /bin/bash deploy",
        "mkdir -p /opt/enterprise-app",
        "chown deploy:deploy /opt/enterprise-app",
        "echo 'deploy ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/deploy"
      ]
    },
    {
      "type": "shell",
      "inline": [
        "systemctl enable ssh",
        "systemctl start ssh"
      ]
    }
  ],
  "post-processors": [
    {
      "type": "manifest",
      "output": "manifest.json",
      "strip_path": true
    }
  ]
}
